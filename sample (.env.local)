# ------------------------------------------------------------------------------
# This is .env.local file.
#
# All of these environment vars must be defined either in your environment or in
# a local .env file in order to run this app in production or development.
#
# INFO: To run locally, change the "DB_HOST" variable to "localhost".
# To run in docker, change "DB_HOST" to database container name:
# ------------------------------------------------------------------------------

# Port application:
NEXT_PUBLIC_PORT=3000

# Variable:
NODE_ENV='development'

# Base URL to API:
NEXT_PUBLIC_API_BASE_URL='http://localhost:3000/api/v1/'
